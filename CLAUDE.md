- IMPORTANT: Do not build unless told to do so.
- IMPORTANT: Do not push unless told to.
- IMPORTANT: Do not check Vercel deployment status unless told to.

- Do not put env in gitignore.
- Allow env vars to be committed and pushed.
- Always use pnpm instead of bun or npm.
- Show first 7 chars of commit hash at end of git push.

## Tailwind

- Tailwind v4 does NOT use a config file. There is no tailwind.config.ts.

## Design

- Anytime a modal or drawer is open, the background should be blurred, not black.
- Do not check vercel deployment after push
- At the end of every change in claude code, show how much real time claude code took to complete the change
- Do not push unless I tell you so.
- Do not push unless told to do so.